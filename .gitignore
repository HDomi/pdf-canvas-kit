node_modules/
dist/
*.tsbuildinfo

# pdf.js 런타임 자산은 postinstall 이 복사한다. 커밋하지 않는다 (ARCHITECTURE §4.1)
demo/public/pdfjs/

# npm run fixtures 로 생성한다. 100페이지 픽스처는 수 MB다
demo/fixtures/

# 로컬 확인용 PDF
temp.pdf
*.local.pdf

.DS_Store

# 데모 빌드 산출물
dist-demo/

# 예제 앱 — pdf.js 자산은 `npm run dev` 가 복사한다 (scripts/dev-examples.mjs)
examples/*/public/pdfjs/
examples/*/dist/

# 개인 배포 스크립트. 토큰·레지스트리 설정을 담을 수 있어 커밋하지 않는다.
publish.sh
publish-*.sh

# npm 인증 토큰이 들어간다. 절대 커밋하지 않는다.
.npmrc
