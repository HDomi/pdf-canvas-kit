# CLAUDE.md

이 저장소에서 작업할 때 지켜야 하는 규칙. 사람과 AI 에이전트 모두에게 적용된다.

---

## 1. 문서 4종 — 무엇을 어디에 쓰는가

| 문서 | 담는 것 | 담지 않는 것 |
| --- | --- | --- |
| **ARCHITECTURE.md** | 무엇을 어디서 바꾸는가 — 레이어, 상수·토큰 위치, 좌표계 규칙, Ports, **설계 근거** | 사용법, 일정 |
| **README.md** | 첫 화면 — 설치, ⚠️ 함정 둘, docs 인덱스, 개발 실행 | 상세 사용법 (docs 로 링크) |
| **docs/*.md** | 소비자 사용 설명서 — 환경별 적용, 커스터마이징, API, 함정 | 내부 구조 |
| **docs/TODO.md** | 남은 일. **끝난 것은 지운다** | 완료 이력 |

네 문서는 서로 링크한다. 같은 내용을 두 곳에 복사하지 말고 한 곳에 쓰고 링크한다.
**README 는 짧게 유지한다** — 길어지면 `docs/` 로 옮기고 링크만 남긴다.
`npm run check:docs` 가 죽은 상대 링크를 잡는다 (lint 게이트에 포함).

> **`PLAN.md` 는 2026.08.21 에 삭제했다.** 배포 단계로 넘어가면서 마일스톤·일정 문서가 필요
> 없어졌다. 설계 결정의 근거는 **코드 주석과 ARCHITECTURE 로 옮겼다** — 주석에 출처 표시를
> 남기는 대신 **그 결정 내용을 한 구절로 적는다.** 근거는 코드 옆에 있는 편이 낫다.
>
> ```ts
> // ✗ 출처만 있다. 문서가 사라지면 아무것도 안 남는다
> /** 커스텀 객체 타입 (PLAN D25) */
> // ✓ 근거가 주석 안에 있다
> /** 커스텀 객체 타입 (커스텀 객체는 소비자가 정의한다 — 코어에 도메인이 없다) */
> ```

---

## 2. 변경할 때 반드시 함께 갱신한다

**코드를 바꿨다면 아래 표에서 해당 행을 찾아 문서를 같은 커밋에서 갱신한다.**
문서 갱신이 빠진 변경은 미완성으로 취급한다.

| 바꾼 것 | ARCHITECTURE.md | README / docs |
| --- | --- | --- |
| 설계 방향·기술 선택 | **해당 절에 근거를 적는다** (왜, 버린 대안) | — |
| 미결정 사항 결정 | 결론과 근거를 반영 | 사용법 바뀌면 |
| 상수 값 (`defaults.ts`) | **§2 표 · §5 수치** | — |
| CSS 토큰 (`tokens.css`) | **§3** | 오버라이드 예제 바뀌면 |
| 공개 API (`src/index.ts`) | **§9** | **최소 예제** |
| Ports 인터페이스 | **§7** | 주입 예제 |
| 새 디렉토리·파일 | **§8 트리** | — |
| ESLint 아키텍처 규칙 | **§10** | — |
| 마일스톤 완료 | 구현된 것 반영 | 실행 방법 |
| 성능·용량 실측 | **§5** | — |
| 새 npm 스크립트 | 필요하면 | **스크립트 표** |
| 새 의존성 | — | — |

### 2.1 버전·날짜
- ARCHITECTURE 헤더의 `arch-x.y` 와 `최종 수정일` 을 갱신한다
- 상대 날짜("지난주")를 쓰지 않는다. 절대 날짜로 쓴다

---

## 3. 코드 규칙

### 3.1 언어
- **코드 주석·JSDoc은 한국어.** (2026.08.19 결정) 이 저장소의 관례다. 새 파일도 한국어로 쓴다
- **문서(ARCHITECTURE·ARCHITECTURE·README)는 한국어.** 기획서가 한국어다
- **식별자·타입명은 영어.** 코드 자체는 영어로 읽히는 편이 표준 도구·검색과 맞는다
- **커밋 메시지는 한국어.** (2026.08.20 결정, 이전 규칙 뒤집음) 접두사만 영어로 남긴다 — §5
- **로그·에러 메시지는 영어.** 스택트레이스·이슈 검색에서 다뤄지는 문자열이다.
  단 사용자에게 보이는 문구는 `core/config/strings.ts` 를 쓴다(하드코딩 금지 — 호스트가 번역할 수 있어야 한다)
- 코드 예시 안의 주석도 한국어

### 3.2 JSDoc — 무엇을 쓰는가
타입이 이미 말하는 것을 반복하지 않는다. **타입이 말할 수 없는 것**을 쓴다.

```ts
// 나쁨 — 타입의 반복
/** 배율을 설정한다. @param scale 배율 */
function zoomTo(scale: number) {}

// 좋음 — 왜, 그리고 함정
/**
 * `anchor` 지점을 화면에 고정한 채 배율을 바꾼다.
 *
 * 순서가 중요하다. `scale` 을 바꾸면 페이지 프레임 크기가 변해 스크롤 범위가 달라지므로,
 * 스크롤 보정은 **레이아웃이 반영된 뒤**에 해야 한다. 먼저 하면 이전 최대값에 걸려 잘린다.
 */
async function zoomTo(scale: number, anchor: Point) {}
```

반드시 적는 것:
- **왜 이 방식인지** (대안을 버린 이유)
- **호출 순서·타이밍 제약**
- **단위** (pt / CSS px / 라스터 px — §3.3)
- **신뢰할 수 없는 값** (`diagnose.ts`처럼 추측이 섞이면 명시)
- **실측 근거가 있는 상수**는 수치와 조건

### 3.3 좌표 단위
- 저장·전달되는 좌표는 **pt**다. 변수명·JSDoc에 단위를 남긴다
- 화면 px 값은 이름에 `Px` 를 붙인다 (`sizePx`, `thresholdPx`)
- 변환은 `geometry/units.ts` 4함수만 쓴다. 직접 계산 금지

### 3.4 아키텍처 경계 (ESLint가 막는다)
- `src/core/**` 는 `vue`·`@vueuse/*`·`*.vue` 를 import 하지 않는다
- `src/vue/editor/objects/**` 는 `geometry/units` 를 import 하지 않는다

린트를 우회하지 말고, 경계를 바꿔야 하면 ARCHITECTURE에 D#로 남기고 규칙을 고친다.

### 3.5 상수
새 매직 넘버를 코드에 박지 않는다. `src/core/config/defaults.ts` 의 세 그룹 중 하나에 넣는다.

| 그룹 | 성격 | 변경 가능성 |
| --- | --- | --- |
| `LIMITS` | 기획 스펙 | **서버와 동일해야 함** |
| `EDITOR_DEFAULTS` | 편집 ergonomics | 제품별 조정 가능 |
| `RENDER_DEFAULTS` | 화질·성능 | 실측 후 조정 |

시각 값은 상수가 아니라 `tokens.css` 의 CSS 변수로 넣는다(ARCHITECTURE §3.2).

### 3.6 타입 export
외부에서 쓸 수 있어야 하는 타입은 `src/index.ts` 에서 재export한다.
export를 추가하면 ARCHITECTURE §9 예제도 갱신한다.

---

## 4. 검증

자동 테스트 러너가 없다 — 검증 화면으로 대체한다. 커밋 전에 아래를 통과시킨다.

```bash
npm run typecheck     # vue-tsc + tsc (node config)
npm run lint          # eslint + prettier --check
npm run build         # 라이브러리 빌드 + d.ts
npm run checks        # 검증 케이스 364건 (브라우저 없이. 실패 시 exit 1)
```

그리고 **직접 확인한다**:
- 순수 함수·반응성을 바꿨으면 `npm run checks` — 새 동작은 케이스를 **추가**한다
  (`demo/checks/allCases.ts` 가 단일 출처)
- PDF·렌더를 바꿨으면 `/spike/` 에서 픽스처 전부
- UI를 바꿨으면 해당 마일스톤의 DoD 항목

### 4.1 실측 없이 성능·용량을 주장하지 않는다
"더 빠르다", "더 작다" 를 쓰려면 측정한다. 측정했으면 **조건과 편향까지** 기록한다.
ARCHITECTURE / ARCHITECTURE §5 가 그 형식이다.

예: WebP가 용량은 44% 작지만 인코딩이 6.7배 느리다는 사실은 측정으로만 나왔고,
픽스처가 벡터 텍스트라 PNG에 유리한 편향이 있다는 것도 함께 적혀 있다.

### 4.2 확인하지 않은 것을 확인한 것처럼 쓰지 않는다
브라우저에서 실행해 보지 않았으면 "동작한다"고 쓰지 않는다.
실패한 검증·건너뛴 항목·남은 위험은 짧게라도 반드시 보고한다.

---

## 5. 커밋

- 한 커밋은 한 가지 변경. 코드 + 그에 딸린 문서 갱신은 **같은 커밋**
- **메시지는 한국어** (2026.08.20 결정). 접두사는 영어로 유지한다 —
  `feat` / `fix` / `refactor` / `docs` / `chore`. 도구가 읽는 구조이고 관례가 굳어 있다
- 파괴적 변경은 `!` 를 붙인다 (`feat!:`)
- 본문에 **왜** 를 쓴다. 무엇을 바꿨는지는 diff가 말한다
- 실패·건너뛴 검증·남은 위험을 본문에 남긴다. 커밋 메시지가 그 시점의 유일한 기록이다

식별자·경로·에러 문자열은 원문 그대로 둔다. 번역하면 검색이 안 된다.

```
fix(pdf): CMap·표준 폰트 URL 설정

pdf.js 는 이 자산을 런타임에 가져온다. cmaps/ 가 없으면 임베드되지 않은 CID 폰트의
글자가 조용히 전부 사라진다. configurePdfResources() 와 복사 스크립트를 추가하고,
cMapUrl 이 설정되지 않았을 때 경고를 낸다.
```

---

## 6. 자주 하는 실수

| 실수 | 왜 문제인가 |
| --- | --- |
| 객체 렌더에서 `x * scale` 계산 | 배율은 페이지 컨테이너 `transform` 한 곳에만. 이중 적용된다 |
| 좌표 변환에 `scrollLeft` 더하기 | `getBoundingClientRect()`에 이미 포함돼 있다. 중복 가산된다 |
| 드래그 중 문서 상태 갱신 | undo 스택이 픽셀 단위로 오염된다. `pointerup`에 1회 커밋 |
| 뷰 상태(배율·스크롤)를 문서에 저장 | 배율만 바꿔도 자동저장이 돌고, undo가 배율을 되돌린다 |
| blob URL을 `origin: 'inline'` 으로 반환 | 직렬화 가드가 무력화돼 죽은 링크가 저장된다 |
| `LIMITS` 를 서버와 다르게 바꾸기 | 교사가 만든 워크시트를 백엔드가 거부한다 |
| 상수를 코드에 박기 | 어디서 바꿔야 할지 아무도 못 찾는다 |
